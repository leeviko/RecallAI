import Button from '@/components/buttons/Button';

const Page = () => {
  return (
    <div>
      <Button color="accent-dark" variant="fill" href="/create">
        Create new deck
      </Button>
    </div>
  );
};

export default Page;
